<!-- Copyright (c) 2026, Richard Thomson -->

# Release 5 Plan

## Goal

Ship trn 5.0 as the first CMake-native release of the revived codebase.
The release should be buildable from current instructions, pass the
supported CI matrix, avoid known crash-class bugs, and provide a usable
Unix and Windows package story.

The release is not a full rewrite.  The line is:

- fix release-blocking correctness, build, and packaging defects;
- finish the CMake migration for current runtime policy;
- add enough deterministic test infrastructure to modernize NNTP safely;
- implement the NNTP and posting pieces needed for current use;
- defer broad internal cleanup unless it unlocks one of those goals.

## Current State

The INI refactor described in `ini-words.md` is implemented in current
source.  `IniSchema`, `IniDocument`, `IniSectionValues`, `OptionCatalog`,
`OptionDraft`, `OptionApplier`, `DataSourceConfig`, and `RcGroupConfig`
are present, wired into `libtrn`, and covered by focused tests.

The old `FILE_REF` macro task is also complete enough for this plan.
Source uses `file_ref`, and the config target has direct tests for it.

Those two areas should be treated as release verification only, not as
active feature slices.

## Script Audit

Repo-owned shell work excludes the `vcpkg` submodule.  The current
runtime shell templates that need a Windows batch, PowerShell, or native
replacement strategy are:

- `config/data/Pnews.in`: interactive news poster; sources
  `Pnews.header`, calls `Speller`, `mbox.saver`, `trn-artchk`,
  `nntplist`, `inews`, the editor, the pager, and optional PGP tools.
- `config/data/Pnews.header.in`: shell scriptlet sourced by `Pnews`;
  include it in the `Pnews` replacement rather than treating it as a
  standalone command.
- `config/legacy/Rnmail.SH`: reply and forward mailer; not yet
  generated by CMake, and needs a Windows strategy with `Pnews`.
- `config/data/Speller.in`: spell-check wrapper using `awk`, `sed`,
  `diff`, `ed`, `spell` or `ispell`, `pr`, a pager, and temp files.
- `config/data/filexp.in`: tilde and install-prefix path expansion using
  `sed`, `expr`, `/etc/passwd`, and optional `/bin/csh` globbing.
- `config/data/mbox.saver.in`: mailbox append helper using shell
  redirection, `tail`, and `sed`.
- `config/data/norm.saver.in`: normal save helper using shell
  redirection and `tail`.

Generated data files do not need Windows executable equivalents:
`config/data/newsnews.in`, `config/data/access_def.in`, and
`config/data/environment.in`.  However, `environment.in` and generated
config defaults still reference shell commands and must get Windows-safe
defaults where the feature is supported: `NEWSPOSTER`, `MAILPOSTER`,
`MBOXSAVER`, `NORMSAVER`, `CANCEL`, `EXSAVER`, `PIPESAVER`, `UNSHAR`,
`PREF_SHELL`, and `SHAR_SAVER`.

Legacy build scripts are not runtime Windows porting targets:
`config/legacy/Configure`, `config/legacy/Makefile.SH`, and
`config/legacy/hints/*.sh` should be retired from the active build path
or kept only as historical reference.

## GitHub 5.0 Milestone

The GitHub 5.0 milestone is the mandatory release scope.  Every issue on
that milestone is a release gate.  Open issues must be fixed before the
release is cut, unless the issue is explicitly removed from the
milestone on GitHub.  Closed issues are mandatory regression checks.

Open mandatory milestone issues:

- #3: Add nntps/SSL support.
- #5: Support decoding yEnc files.
- #14: Make trn available as a Windows 32-bit/64-bit application.
- #16: Ensure posting works properly.
- #17: Restore original character encoding conversions.
- #18: Update the INSTALL file.
- #19: All previous Configure script options should be CMake configure
  options.
- #23: Build on macOS.
- #24: `"buffer overflow detected"` when local newsgroup source is
  missing `Group Desc`.
- #31: All files previously generated by configure script should be
  generated by CMake.
- #33: Test plan.
- #34: Add a `"UTF-8 => ASCII"` code conversion.
- #38: Diagnose `compress_name` disabled tests, re-enable and pass them.

Closed milestone issues remain mandatory release verification items.  The
release must not regress #2, #9, #11, #12, #20, #28, #32, #36, or #39.

## Priorities

### P0: Must Finish Before 5.0

P0 is the GitHub 5.0 milestone.  No open P0 issue may be deferred to 5.1
while it remains on that milestone.  Closed P0 issues must remain fixed.

1. Fix release identity, readme reconciliation, and install docs: #18.
2. Finish CMake and generated-file migration: #19, #31.
3. Fix mandatory crash and disabled-test issues: #24, #38.
4. Ship Windows and macOS release support: #14, #23.
5. Ship NNTP TLS/nntps and deterministic release tests: #3, #33.
6. Fix posting and mandatory charset/decode work: #5, #16, #17, #34.
7. Verify closed milestone work stays complete: #2, #9, #11, #12, #20,
   #28, #32, #36, #39.

### P1: Supporting Release Work

P1 work is not itself on the 5.0 milestone, but may be needed to finish
or stabilize P0 work.

1. Investigate the threaded-article save crash report: #10.
2. Replace remaining DOS-as-Windows platform decisions.
3. Handle platform path lists correctly: #27.
4. Complete Windows helper scripts when needed by Windows packaging: #29.
5. Modernize core NNTP command selection when needed by #3.
6. Add enough test automation to make #33 meaningful.

### P2: Non-Milestone Follow-Up

P2 work should not delay 5.0 unless it is moved onto the GitHub 5.0
milestone.

1. Add moderators support: #25.
2. Fix the UTF-8 underline repro if it is not covered by #17 or #34:
   #30.
3. Refactor AddGroup storage and sort unsubscribed-group listings: #15.
4. Use archive.org as additional test content: #35.

## Implementation Slices

Use these as review units.  Each numbered item owns at most one open
GitHub issue.  Closed milestone issues are not implementation items; they
are captured in the regression checklist.  A large open issue can span
several numbered items; close it only after its final acceptance line is
met.

### 1. Readme Reconciliation

Issues: none.

Milestone status: P0 support; local files still diverge.

Files likely touched:

- `README.txt`
- `ReadMe.md`

Tasks:

- Choose one canonical readme for the release.
- Merge useful historical overview text from `README.txt`.
- Preserve current CMake, CI, UTF-8, posting, and contribution text from
  `ReadMe.md`.
- Preserve packaging-status, build, release, standard, and license
  badges that still apply.
- Remove the stale duplicate, or make any retained compatibility file a
  short pointer that cannot drift.

Acceptance:

- There is one authoritative readme for the release.
- `README.txt` and `ReadMe.md` no longer contain divergent content.
- The canonical readme includes the modern build and project overview.

### 2. Runtime Release Identity

Issues: none.

Milestone status: P0 support.

Files likely touched:

- `CMakeLists.txt`
- `libtrn/include/trn/patchlevel.h`

Tasks:

- Make CMake project version and runtime version reporting agree.
- Set the release identity to `5.0.0`.
- Remove stale release-facing references to `4.0-test77`.

Acceptance:

- Runtime version output and CMake package metadata agree.
- No release-facing file presents 4.0-test77 as current.

### 3. Install Quickstart

Issues: #18.

Milestone status: open mandatory 5.0 issue.

Files likely touched:

- `INSTALL.txt`
- `INSTALL.md`

Tasks:

- Migrate `INSTALL.txt` to `INSTALL.md` as the canonical install
  document.
- Replace stale trn 4.0 install text with CMake preset instructions.
- Document required tools, submodules, and vcpkg bootstrap steps.
- Document default configure, build, test, install, and package commands.
- Leave `INSTALL.txt` only as a short pointer if compatibility needs it.
- State which platforms are intended to work for 5.0.

Acceptance:

- `INSTALL.md` is the authoritative install document.
- A fresh checkout can be built from the `INSTALL.md` quickstart alone.
- No install document describes the old Configure workflow.

### 4. Configure Option Inventory

Issues: #19.

Milestone status: open mandatory 5.0 issue.

Plan source: `config.md`.

Files likely touched:

- `config/cmake/configure_trn.cmake`
- `config/cmake/config.h.in`
- `config/include/config/common.h`
- configure-option tests

Tasks:

- Compare old Configure options with current CMake inputs.
- Classify each option as needed, obsolete, already handled, or removed.
- Identify each source-consumed config symbol without a CMake owner.
- Keep irrelevant 1990s portability probes retired.

Acceptance:

- The remaining #19 work is explicit and testable.
- No source-consumed config symbol is missing from the inventory.

### 5. CMake Cache Options

Issues: #19.

Milestone status: open mandatory 5.0 issue.

Depends on: 4.

Files likely touched:

- `config/cmake/configure_trn.cmake`
- `config/cmake/config.h.in`
- `config/include/config/common.h`
- config tests

Tasks:

- Add CMake cache variables for the relevant retained options.
- Route `ACTIVE_TIMES` into generated config.
- Configure or retire `HAS_RDCHK`, `HAS_SIGBLOCK`, `HAS_SIGHOLD`,
  `UNION_WAIT`, and `USE_WIFSTAT`.
- Emit `I_SGTTY` only if source still needs it.

Acceptance:

- Each retained Configure option has a CMake cache variable.
- Retired options have no live source dependency.
- Generated config tests cover the new variables.

### 6. Configure Defaults And BSD Checks

Issues: #19.

Milestone status: open mandatory 5.0 issue.

Regression checks: #28.

Depends on: 5.

Files likely touched:

- `config/cmake/configure_trn.cmake`
- config tests

Tasks:

- Convert `HOST_BITS` from boolean to integer-count semantics.
- Preserve fallback behavior for empty path and file defaults.
- Revisit Linux name defaults such as `PASS_NAMES` and
  `BERKELEY_NAMES`.
- Keep BSD behavior based on feature checks instead of host-name checks.

Acceptance:

- `HOST_BITS` can represent values greater than one.
- Empty defaults do not bypass intended source fallbacks.
- BSD configure behavior from #28 remains fixed.

### 7. Install Option Documentation

Issues: #18.

Milestone status: open mandatory 5.0 issue.

Depends on: 5, 6, and 10.

Files likely touched:

- `INSTALL.md`

Tasks:

- Document the supported CMake cache variables from #19.
- Document generated helper files from #31.
- Document release-supported package and install layouts.
- Add known limits for deferred non-milestone work.

Acceptance:

- `INSTALL.md` describes the CMake options needed to customize trn.
- #18 can close after the quickstart and option reference are reviewed.

### 8. Generated Helper Inventory

Issues: #31.

Milestone status: open mandatory 5.0 issue.

Plan source: `config.md`.

Files likely touched:

- `config/legacy`
- `config/data`
- `config/data/CMakeLists.txt`

Tasks:

- List remaining `config/legacy/*.SH` runtime outputs.
- Decide which helpers are installed, generated, or retired.
- Identify helper substitutions still using metaconfig residue.

Acceptance:

- Every legacy helper has an explicit 5.0 disposition.
- The remaining #31 implementation list is small and reviewable.

### 9. Remaining Helper Generation

Issues: #31.

Milestone status: open mandatory 5.0 issue.

Depends on: 8.

Files likely touched:

- `config/data/Rnmail.in`
- `config/data/mbox.saver.in`
- `config/data/norm.saver.in`
- `config/data/filexp.in`
- `config/data/Speller.in`
- `config/data/CMakeLists.txt`

Tasks:

- Add a generated `Rnmail` replacement.
- Finish `mbox.saver`, `norm.saver`, `filexp`, and `Speller`.
- Remove leftover `nidump`, `ypmatch`, and obsolete substitutions.
- Keep Unix helper behavior compatible where it still matters.

Acceptance:

- Runtime helper files are generated by CMake.
- Installed defaults do not point at `config/legacy/*.SH`.

### 10. Helper Install Rules

Issues: #31.

Milestone status: open mandatory 5.0 issue.

Depends on: 9.

Files likely touched:

- `config/data/CMakeLists.txt`
- `CMakeLists.txt`
- package support files
- helper tests

Tasks:

- Install generated helpers with executable permissions on Unix.
- Include generated helpers in packages.
- Add tests that assert helper paths point at generated outputs.

Acceptance:

- All files previously generated by Configure are generated by CMake.
- Package and install trees contain the helper files needed at runtime.

### 11. Local Group Desc Crash Repro

Issues: #24.

Milestone status: open mandatory 5.0 issue.

Files likely touched:

- `tests/test_datasrc.cpp`
- access-file fixtures
- source files found by the repro

Tasks:

- Add a local spool fixture without `Group Desc`.
- Reproduce the 4.0-test77 crash path in the current typed INI flow.
- Capture expected behavior when `Group Desc = none` is omitted.

Acceptance:

- The #24 access-file shape has a focused regression test.
- The test fails or exposes a clear missing-path diagnostic before fix.

### 12. Local Group Desc Crash Fix

Issues: #24.

Milestone status: open mandatory 5.0 issue.

Depends on: 11.

Files likely touched:

- `libtrn/DataSourceConfig.*`
- `libtrn/datasrc.cpp`
- access-file tests

Tasks:

- Fix missing group-description handling for local spool sources.
- Preserve explicit `Group Desc = none` behavior.
- Make the default safe when no group-description source exists.

Acceptance:

- The #24 repro no longer crashes.
- Local spool sources work with and without `Group Desc = none`.

### 13. Compress Name Contract

Issues: #38.

Milestone status: open mandatory 5.0 issue.

Files likely touched:

- `tests/test_rt-util.cpp`
- source file containing `compress_name`

Tasks:

- Re-enable the four disabled `CompressNameTest` cases.
- Confirm whether `max` means display width instead of buffer size.
- Fix implementation or tests so the contract is explicit.

Acceptance:

- No `DISABLED_` tests remain for #38.
- The test names describe the expected display-width behavior.

### 14. Threaded Save Crash Triage

Issues: #10.

Milestone status: not on the 5.0 milestone.

Files likely touched:

- threaded article save tests
- source files found by the repro

Tasks:

- Try to reproduce save-threaded-articles exiting with no error.
- Add diagnostics that preserve the prompt escape reason.
- Fix the crash if the repro can be made deterministic.

Acceptance:

- #10 has either a fixed repro or enough diagnostic data to defer.
- This work does not block 5.0 unless the issue joins the milestone.

### 15. Posting Header Harness

Issues: #16.

Milestone status: open mandatory 5.0 issue.

Regression checks: #20.

Files likely touched:

- `config/data/Pnews.in`
- `config/data/Pnews.header.in`
- posting tests

Tasks:

- Add tests around generated posting headers.
- Preserve MIME header generation already present in `Pnews`.
- Preserve User-Agent generation from #20.

Acceptance:

- Posted articles include the expected User-Agent header.
- Header tests are in place before transfer-encoding changes.

### 16. Posting Transfer Encoding

Issues: #16.

Milestone status: open mandatory 5.0 issue.

Regression checks: #20.

Depends on: 15.

Files likely touched:

- `config/data/Pnews.in`
- `inews`
- posting tests

Tasks:

- Replace unconditional `Content-Transfer-Encoding: 8bit`.
- Choose `7bit` for ASCII-only text.
- Choose `8bit` only when the body needs it.
- Update release docs that still call posting half-working.

Acceptance:

- ASCII-only posts are not declared as `8bit`.
- Non-ASCII posts still get a valid transfer encoding.
- #16 can close without regressing #20.

### 17. Platform Path Lists

Issues: #27.

Milestone status: not on the 5.0 milestone.

Plan source: `msdos.md`.

Files likely touched:

- `libtrn/util.cpp`
- `libtrn/rcstuff.cpp`
- platform tests

Tasks:

- Add path-list helpers for Unix `:` and Windows `;` separators.
- Apply the helpers to settings such as `MAILCAPS`.
- Keep Windows drive letters from being parsed as list separators.

Acceptance:

- Windows paths with drive letters do not break path-list parsing.
- The helper can be reused by Windows release work.

### 18. Modern Windows Platform Policy

Issues: #14.

Milestone status: open mandatory 5.0 issue.

Plan source: `msdos.md`.

Depends on: 17.

Files likely touched:

- `config/cmake/configure_trn.cmake`
- `config/include/config/msdos.h`
- `config/include/config/config2.h`
- `libtrn/terminal.cpp`
- `libtrn/util.cpp`
- platform tests

Tasks:

- Stop defining `MSDOS` merely because `WIN32` is true.
- Replace console input branches with named console-input capability.
- Replace terminal-mode branches with named terminal capability.
- Replace shell launch branches with a process-launch service.
- Replace filename and permission behavior with platform policy.

Acceptance:

- `MSDOS` no longer means modern Windows.
- Remaining platform branches name their actual feature or policy.
- Windows console and path behavior have focused tests.

### 19. Windows Helper Strategy

Issues: #29.

Milestone status: not on the 5.0 milestone.

Depends on: 9.

Files likely touched:

- `config/data`
- `config/data/CMakeLists.txt`
- `config/cmake/config.h.in`
- Windows helper tests
- package support files

Tasks:

- Define Windows equivalents or native replacements for `Pnews`,
  `Rnmail`, `Speller`, `filexp`, `mbox.saver`, and `norm.saver`.
- Fold `Pnews.header` into the `Pnews` replacement strategy.
- Decide whether each entry point is `.cmd`, `.ps1`, native C++, or
  unsupported on Windows for 5.0.
- Give Windows-safe defaults for `NEWSPOSTER`, `MAILPOSTER`,
  `MBOXSAVER`, `NORMSAVER`, `CANCEL`, `EXSAVER`, `PIPESAVER`,
  `UNSHAR`, `PREF_SHELL`, and `SHAR_SAVER`.
- Keep generated data files as data: `newsnews`, `access_def`, and
  `environment`.
- Avoid requiring a Unix shell compatibility layer for normal use.
- Preserve `EDITOR` and `VISUAL` behavior where practical.

Acceptance:

- Windows helper policy is explicit.
- Every runtime shell template in the audit has a Windows disposition.
- Windows defaults do not point at `/bin/sh`, Unix `tail`, or Unix-only
  helper scripts for supported features.
- #14 packaging can depend on generated Windows-safe helpers.

### 20. Windows Package Artifacts

Issues: #14.

Milestone status: open mandatory 5.0 issue.

Depends on: 2, 7, 10, 18, and 19.

Files likely touched:

- `CMakeLists.txt`
- `CMakePresets.json`
- package support files
- CI workflow files

Tasks:

- Define install contents for binaries, data files, and helpers.
- Add CPack or equivalent package presets.
- Produce a Windows portable ZIP.
- Produce a Windows installer if the packaging toolchain is available.

Acceptance:

- Windows users can install or unpack without a Unix shell layer.
- Package artifacts contain all files needed for normal use.

### 21. macOS Terminal Include Fix

Issues: #23.

Milestone status: open mandatory 5.0 issue.

Files likely touched:

- `libtrn/terminal.cpp`
- terminal headers
- macOS configure or CI files

Tasks:

- Reproduce the old macOS `<curses.h>` include conflict.
- Isolate curses and terminal includes behind stable headers.
- Keep Unix terminal behavior unchanged.

Acceptance:

- macOS can compile the terminal sources.
- The fix is covered by CI or a documented focused build.

### 22. macOS CI Gate

Issues: #23.

Milestone status: open mandatory 5.0 issue.

Depends on: 21.

Files likely touched:

- `.github/workflows`
- `CMakePresets.json`

Tasks:

- Re-enable macOS CI after the terminal include fix.
- Keep macOS configure, build, and tests in the supported matrix.
- Document any macOS package limitation that remains for 5.0.

Acceptance:

- macOS CI is green for the release branch.
- #23 can close without weakening Unix coverage.

### 23. Manual Test Plan Inventory

Issues: #33.

Milestone status: open mandatory 5.0 issue.

Files likely touched:

- test plan docs
- tests

Tasks:

- Convert the #33 checklist into a release verification matrix.
- Mark each item as automated, manual, deferred, or obsolete.
- Prioritize access files, KILL files, posting, command-line options,
  macros, `%` expansion, and extraction.

Acceptance:

- #33 has a tracked checklist tied to concrete tests or manual steps.
- No high-risk checklist item is unclassified.

### 24. Deterministic NNTP Fixtures

Issues: #33.

Milestone status: open mandatory 5.0 issue.

Plan sources:

- `mock-nntp-server.md`
- `nntp-test-server.md`

Files likely touched:

- `tests/support/MockNntpServer.*`
- `tests/support/nntp_test_server.py`
- `tests/support/run_nntp_fixture.py`
- `tests/CMakeLists.txt`
- test data files

Tasks:

- Add a C++ loopback mock with ordered expectations.
- Add a Python fixture server with JSON config and port-file output.
- Cover `LIST`, `GROUP`, `LISTGROUP`, article retrieval, overview,
  headers, posting capture, and dot-stuffing.

Acceptance:

- NNTP tests need no external server.
- Protocol tests are deterministic under parallel test execution.

### 25. Manual Check Automation Seed

Issues: #33.

Milestone status: open mandatory 5.0 issue.

Depends on: 23 and 24.

Files likely touched:

- tests
- test data
- release checklist docs

Tasks:

- Automate the highest-risk #33 manual checks.
- Add one short scripted `trn` read-one-article test.
- Add one short end-to-end `nntplist` test.
- Leave lower-risk manual checks visible in the release checklist.

Acceptance:

- The release checklist has automated coverage for critical paths.
- Manual-only items are deliberate, named, and repeatable.

### 26. NNTP Capability Model

Issues: #3.

Milestone status: open mandatory 5.0 issue.

Regression checks: #9.

Plan source: `nntp-legacy-conversations.md`.

Depends on: 24.

Files likely touched:

- `nntp/nntpinit.cpp`
- `nntp/nntpclient.cpp`
- `libtrn/nntp.cpp`
- NNTP tests

Tasks:

- Add an `NntpCapabilities` data type.
- Parse multiline `CAPABILITIES` replies.
- Store capability state in the NNTP session.
- Refresh capabilities after `MODE READER` when needed.

Acceptance:

- Capability parsing is tested for advertised and missing features.
- Overview performance behavior from #9 is not regressed.

### 27. NNTP Modern Commands

Issues: #3.

Milestone status: open mandatory 5.0 issue.

Regression checks: #9.

Depends on: 26.

Files likely touched:

- `libtrn/rt-ov.cpp`
- `libtrn/head.cpp`
- `libtrn/datasrc.cpp`
- NNTP tests

Tasks:

- Send `MODE READER` only when needed or advertised.
- Prefer `OVER` before `XOVER`.
- Prefer `HDR` before `XHDR`.
- Prefer `LIST NEWSGROUPS group` before `XGTITLE`.
- Avoid repeated unsupported-command probes within a session.

Acceptance:

- Modern servers use advertised RFC 3977 commands.
- Old servers still work through explicit fallback paths.
- Overview retrieval does not regress #9.

### 28. NNTP STARTTLS

Issues: #3.

Milestone status: open mandatory 5.0 issue.

Depends on: 26.

Files likely touched:

- `nntp`
- `libtrn/DataSourceConfig.*`
- `libtrn/datasrc.cpp`
- NNTP tests

Tasks:

- Add cleartext and STARTTLS session state.
- Parse the `STARTTLS` capability.
- Handle response `483` by attempting TLS when allowed.
- Add config policy for requiring TLS before auth or posting.

Acceptance:

- STARTTLS reading paths are covered by fixture tests.
- Password auth can be protected by TLS policy.

### 29. NNTP Implicit TLS

Issues: #3.

Milestone status: open mandatory 5.0 issue.

Regression checks: #32.

Depends on: 28.

Files likely touched:

- `nntp`
- `libtrn/DataSourceConfig.*`
- `libtrn/datasrc.cpp`
- config data and docs
- NNTP tests

Tasks:

- Add implicit TLS session state.
- Add `nntps` or implicit TLS server configuration.
- Preserve direct SSL/nntps behavior from #32.
- Test direct TLS without a prior cleartext greeting.

Acceptance:

- `nntps` servers can be used for reading.
- Direct SSL/nntps behavior from #32 remains covered.

### 30. NNTP Auth Policy

Issues: #3.

Milestone status: open mandatory 5.0 issue.

Depends on: 28 and 29.

Files likely touched:

- `nntp/nntpauth.cpp`
- `nntp/nntpclient.cpp`
- config data and docs
- NNTP tests

Tasks:

- Drive auth from `AUTHINFO` capability data.
- Use `AUTHINFO USER/PASS` only when advertised or in compatibility
  mode.
- Refuse password auth over cleartext when policy requires TLS.
- Leave SASL as a clean future hook unless a small mechanism is needed.

Acceptance:

- Authentication respects the configured TLS policy.
- #3 can close when command, STARTTLS, nntps, and auth paths pass.

### 31. Legacy Charset Restoral

Issues: #17.

Milestone status: open mandatory 5.0 issue.

Regression checks: #2.

Files likely touched:

- `libtrn/charsubst.cpp`
- `libtrn/mime.cpp`
- `libtrn/art.cpp`
- encoding tests

Tasks:

- Restore original character-set conversions.
- Keep restored conversions from corrupting UTF-8.
- Cover old encodings and mixed UTF-8 paths with tests.

Acceptance:

- #17 behavior is restored.
- Closed UTF-8 support from #2 remains fixed.

### 32. UTF-8 To ASCII Configuration

Issues: #34.

Milestone status: open mandatory 5.0 issue.

Regression checks: #2.

Files likely touched:

- `libtrn/charsubst.cpp`
- config data
- docs
- encoding tests

Tasks:

- Add optional UTF-8 to ASCII substitution.
- Keep the substitution off by default.
- Make the substitution table user supplied or configurable.

Acceptance:

- Users can opt into UTF-8 to ASCII conversion.
- Default UTF-8 display behavior does not change.

### 33. UTF-8 To ASCII Mapping Tests

Issues: #34.

Milestone status: open mandatory 5.0 issue.

Depends on: 32.

Files likely touched:

- encoding tests
- substitution table data

Tasks:

- Cover smart quotes, dashes, spaces, and issue-listed mappings.
- Cover invalid or unknown code points.
- Document the shipped default mapping if one is provided.

Acceptance:

- The #34 mapping table is deterministic and tested.
- #34 can close without weakening UTF-8 baseline behavior.

### 34. YEnc Decode

Issues: #5.

Milestone status: open mandatory 5.0 issue.

Files likely touched:

- `libtrn/decode.cpp`
- `libtrn/uudecode.cpp`
- extraction tests
- fixture articles

Tasks:

- Add yEnc prescan and decode support.
- Add tests for single-part and multipart yEnc payloads.
- Validate filenames through the platform filename policy.
- Keep uuencode and shell archive behavior unchanged.

Acceptance:

- `e` extraction recognizes yEnc articles.
- Existing extraction tests still pass.

### 35. UTF-8 Underline Display

Issues: #30.

Milestone status: not on the 5.0 milestone.

Files likely touched:

- terminal display code
- display tests

Tasks:

- Add a regression using the attached UTF-8 underline message.
- Ensure decoded header display cannot leave terminal attributes active.
- Defer if #17 and #34 do not naturally touch the display path.

Acceptance:

- The underline repro no longer leaves terminal state dirty, or #30 is
  explicitly deferred outside 5.0.

### 36. Moderators Support

Issues: #25.

Milestone status: not on the 5.0 milestone.

Files likely touched:

- `libtrn/DataSourceConfig.*`
- `libtrn/datasrc.cpp`
- posting path
- access templates
- tests

Tasks:

- Add `Moderators File` to the access schema.
- Support local INN-style moderator pattern files.
- Support remote `LIST MODERATORS` when advertised.
- Use wildmat matching for group-to-moderator resolution.

Acceptance:

- Posting to moderated groups uses configured moderator routing.
- This work is deferred unless it joins the 5.0 milestone.

### 37. AddGroup Storage

Issues: #15.

Milestone status: not on the 5.0 milestone.

Plan source: `ToDo.md`.

Files likely touched:

- `libtrn/addng.cpp`
- `libtrn/include/trn/addng.h`
- tests

Tasks:

- Introduce `AddGroupIndex` for selector cursors and page state.
- Store `AddGroup` objects in `std::vector<AddGroup>`.
- Replace linked-list traversal with range helpers.
- Replace temporary home-grown de-duplication storage.

Acceptance:

- AddGroup storage no longer leaks discarded duplicate nodes.
- Object identity is separate from sorted traversal.

### 38. Selector Sorting

Issues: #15.

Milestone status: not on the 5.0 milestone.

Depends on: 37.

Files likely touched:

- `libtrn/rt-page.cpp`
- `libtrn/rt-select.cpp`
- `libtrn/ngsrch.cpp`
- tests

Tasks:

- Make selector paging index-based.
- Add a sortable selector view for unsubscribed newsgroup results.
- Let users change sort order and refine the source glob.

Acceptance:

- Selector paging is index-based.
- Unsubscribed newsgroup listings can be sorted.

### 39. Archive.org Test Samples

Issues: #35.

Milestone status: not on the 5.0 milestone.

Depends on: 23 through 25.

Files likely touched:

- test data
- test documentation

Tasks:

- Use archive.org content only after deterministic fixtures exist.
- Import only small, license-acceptable, pinned test samples.
- Keep downloaded or derived samples reproducible.

Acceptance:

- Archive.org content is optional and not required for default CI.
- Any imported sample has source, license, and reproduction notes.

## Deferred Cleanup

The function-to-member refactors in `StructureFunctions.md` are not 5.0
release gates.  Take them only when they simplify an active numbered
item.

Full SASL support is not a 5.0 gate.  30 should leave a small hook for it
if practical.

Broad replacement of every POSIX file operation with `std::filesystem`
is not a 5.0 gate.  Use it where active numbered items already touch the
code.

The universal selector push/pop cleanup is not a 5.0 gate unless it is
needed by 38.

## Issue Map

Mandatory open GitHub 5.0 milestone issues:

- #3: 26 through 30.
- #5: 34.
- #14: 18 and 20.
- #16: 15 and 16.
- #17: 31.
- #18: 3 and 7.
- #19: 4 through 6.
- #23: 21 and 22.
- #24: 11 and 12.
- #31: 8 through 10.
- #33: 23 through 25.
- #34: 32 and 33.
- #38: 13.

Mandatory closed GitHub 5.0 milestone regression checklist:

- #2: UTF-8 support from acli remains covered.
- #9: overview retrieval performance does not regress.
- #11: packaging status remains visible in the canonical readme.
- #12: the canonical readme preserves the completed update.
- #20: posted articles still include a User-Agent header.
- #28: BSD configure behavior remains feature-test driven.
- #32: direct SSL/nntps behavior remains covered.
- #36: FreeBSD CI support remains present.
- #39: intermittent interpolator crashes remain fixed.

Non-milestone supporting or follow-up issues:

- #10: 14.
- #15: 37 and 38.
- #25: 36.
- #27: 17.
- #29: 19.
- #30: 35.
- #35: 39.

## Release Gate

Release 5.0 can be cut when:

- every open GitHub issue on the 5.0 milestone is closed;
- every closed GitHub issue on the 5.0 milestone remains fixed;
- any issue still open on the 5.0 milestone has first been moved off
  that milestone in GitHub;
- supported CI is green;
- install/package artifacts contain the generated helpers;
- non-milestone P1/P2 work is either complete, documented as deferred,
  or tracked for a later release.
